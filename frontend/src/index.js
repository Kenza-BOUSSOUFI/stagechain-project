// src/index.js
import React from 'react';
import ReactDOM from 'react-dom/client';
import StageChain from './App'; // Aw smiya li derti l App.js

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <StageChain /> 
  </React.StrictMode>
);